# DMA-APB 后续优化方案

## 优化项 1：完善 DMA_LEN 非法输入处理

### 当前设计行为

DMA 长度寄存器 `dma_len` 的位宽为 5 bit，但 APB 写数据 `pwdata` 的位宽为 32 bit：

```verilog
reg [4:0] dma_len;

always @(posedge pclk or negedge presetn) begin
    if (~presetn)
        dma_len <= 0;
    else if (is_dma_len && pwrite == 1 && pready == 1)
        dma_len <= pwdata;
end
```

当前 RTL 将完整的 `pwdata` 赋给 `dma_len`，因此实际只保存 `pwdata[4:0]`。同时，设计将 `0` 或大于 `16` 的长度判断为非法，并通过 `dma_int[2]` 产生中断：

```verilog
assign invalid_value_of_length =
    is_dma_len & pready & (pwdata == 0 | pwdata > 16);
```

这会形成“报告非法长度，但仍将截断后的数值写入寄存器”的行为。例如写入 `32'h0000_0021` 时，DUT 会产生非法长度中断，但 `dma_len` 仍会被更新为 `1`。如果软件在清除中断后继续启动 DMA，可能使用一个并非软件原本期望的长度执行操作。

此外，`invalid_value_of_length` 当前没有判断 `pwrite`。读取 DMA_LEN 寄存器时，`pwdata` 本身没有有效含义，但仍可能因为 `pwdata == 0` 或 `pwdata > 16` 而错误触发非法长度中断。

### 建议优化

将 DMA_LEN 访问拆分为“写握手”和“写数据合法性”两个条件：只有写入值在 `1～16` 范围内时才更新 `dma_len`；非法写入只设置错误中断，不修改原有配置。

```verilog
wire dma_len_write;
wire dma_len_valid;

assign dma_len_write = is_dma_len && pwrite && pready;
assign dma_len_valid = (pwdata >= 1) && (pwdata <= 16);

assign invalid_value_of_length = dma_len_write && !dma_len_valid;

always @(posedge pclk or negedge presetn) begin
    if (~presetn)
        dma_len <= 5'd0;
    else if (dma_len_write && dma_len_valid)
        dma_len <= pwdata[4:0];
    else if ((dma_init_active || dma_copy_active_p2) && slave_pready)
        dma_len <= dma_len - 1'b1;
end
```

优化后的预期行为为：

| APB 操作 | `pwdata` | `dma_len` | `dma_int[2]` |
|---|---:|---|---:|
| 写 DMA_LEN | 1～16 | 更新为写入值 | 0 |
| 写 DMA_LEN | 0 | 保持原值 | 1 |
| 写 DMA_LEN | 大于 16 | 保持原值 | 1 |
| 读 DMA_LEN | 无关 | 不改变 | 不触发 |

### 后续验证补充

UVM 验证环境应分别保留合法测试和异常测试：

- 合法功能测试约束 DMA_LEN 写数据在 `1～16`，覆盖最小值、普通值和最大值。
- 异常测试主动写入 `0`、`17～31`、高位非零值及 `32'hffff_ffff`。
- scoreboard 检查非法写入后 `dma_int[2]` 和 `apb_device_int` 被置位，同时 `dma_len` 保持原值。
- 增加 DMA_LEN 读事务检查，确保读操作不会误触发非法长度中断。
- coverage 分别统计 `zero_invalid`、`minimum_valid`、`normal_valid`、`maximum_valid` 和 `too_large_invalid`，并与中断结果做交叉覆盖。

### 项目说明口径

当前 DUT 版本已经固定，因此验证环境先按照现有 RTL 行为完成检查；同时将上述问题记录为后续设计优化项。该优化体现了验证过程中对寄存器位宽截断、非法配置副作用以及读写条件完整性的进一步分析。

## 优化项 2：避免 DMA busy 期间长期阻塞 CPU/APB 总线

### 当前设计行为

CPU 写入 `DMA_COPY` 并完成 APB 握手后，DUT 在下游继续执行实际复制。如果 CPU 此时发起下一笔 APB 事务，当前 `pready` 逻辑会因为 `is_dma_busy=1` 而保持低电平：

```verilog
else if (!is_dma_read && psel && penable &&
         pready_delay == 0 && !is_dma_busy)
    pready <= 1;
```

APB master 必须持续保持 `PSEL/PENABLE/PADDR/PWRITE/PWDATA`，直到全部 `dma_len` 数据复制完成后才能得到 `PREADY`。这在协议上是合法的 wait state，但会产生以下影响：

- CPU 当前总线访问被阻塞，无法继续执行依赖该 APB 返回的操作。
- 如果该 APB 通路由多个外设共享，长时间 wait state 可能影响其他外设访问。
- CPU 在 DMA busy 期间无法及时读取中断或状态寄存器。
- 如果下游 slave 长时间不返回 `slave_pready`，CPU 侧等待时间也会无限延长。

对于长度较短的教学 DUT，这种实现可以工作；对于强调 DMA 异步执行和系统响应性的实际设计，建议优化。

### 建议的简化优化

DMA 运行期间保持控制通路可响应，不再使用 `!is_dma_busy` 阻塞所有上游访问。推荐按访问类型处理：

| DMA busy 期间的访问 | 建议行为 |
|---|---|
| 读取 `DMA_INT`/`DMA_STATUS` | 正常快速返回 |
| 写 `DMA_INT` 清除中断 | 正常执行 W1C |
| 再次写 `DMA_COPY` 或 `DMA_INIT` | 加入 command FIFO 后再成功握手；或者通过 `PSLVERR` 明确拒绝，不能成功握手后静默丢弃 |
| 写 `DMA_SRC/DST/LEN` | 可写入 shadow 寄存器；若不支持，则继续等待或通过 `PSLVERR` 明确拒绝 |
| direct read/write | 可继续等待、返回明确的 busy/error；若需要并发，则增加对下游 APB 的仲裁 |

可以新增 `DMA_STATUS` 寄存器，至少包含：

```text
DMA_STATUS[0] = busy
DMA_STATUS[1] = command_rejected_while_busy
```

如果不希望增加状态寄存器，也可以在 `dma_int` 中增加一个 `busy_conflict` 中断位。不过，当前 APB 接口没有 `PSLVERR`，如果 DUT 拉高 `PREADY`，CPU 默认会认为命令已经成功接收。因此不能只拉高 `PREADY`、记录一个状态位，然后静默丢弃该命令；除非软硬件规格明确规定 CPU 必须在每次命令后读取拒绝状态。

在不修改 APB 端口的前提下，低成本方案是仅允许 `DMA_STATUS/DMA_INT` 在 busy 期间快速访问，新的 DMA 命令仍保持等待。若还希望新的 DMA 命令不阻塞 CPU，则应加入一个至少一项深度的 command FIFO：只有命令成功进入 FIFO 时才拉高 `PREADY`，当前任务完成后再自动执行该命令。FIFO 已满时继续保持 `PREADY=0`；如果能够扩展接口，也可以增加 `PSLVERR` 明确拒绝。

### 后续验证补充

- 在 DMA copy 进行中读取 `DMA_STATUS/DMA_INT`，检查访问能在规定等待周期内完成。
- busy 期间再次发送 `DMA_COPY/DMA_INIT`，检查当前 DMA 不被覆盖；只有新命令进入 FIFO或收到明确错误响应后，这笔 APB 事务才能结束。
- 检查任何 `PSEL && PENABLE && PREADY` 的成功命令最终都会被执行，不能出现成功握手后静默丢失。
- 检查 busy 期间被拒绝的配置写不会改变当前 `dma_src/dma_dst/dma_len`。
- 覆盖“busy × 访问类型 × 接受/拒绝结果”，并使用 assertion 限制上游最大等待周期。

### 项目说明口径

当前波形中第二次 `PSEL` 持续到整次 DMA copy 完成，是 DUT 使用 APB wait state 进行流量控制的直接结果，协议上合法，但会阻塞 CPU 访问。低成本优化可以先让状态和中断寄存器在 busy 期间保持可访问；如果还要让新命令不阻塞 CPU，则应增加 command FIFO，或者扩展 `PSLVERR` 以明确拒绝，不能把已经成功握手的命令直接作废。
