# DMA-APB 代码理解笔记

> 本笔记基于当前 `apb_device.v` 和 UVM testbench 整理，记录对代码实际行为的理解。DUT 当前版本已经固定，因此文中“注意点”和“优化项”主要用于后续完善验证环境、面试复盘和项目总结。

## 1. 项目整体功能

这个 DUT 不是一个普通的 APB slave。它位于 CPU 和下游存储器之间，同时具有两侧 APB 接口：

```text
UVM master / CPU
        │ 上游APB：APB_MINTF
        ▼
   APB_DEVICE DUT
        │ 下游APB：APB_SINTF
        ▼
UVM slave / memory model
```

DUT 在上游表现为 APB slave，接收 CPU 的寄存器访问和 direct read/write；在下游表现为 APB master，主动访问由 UVM slave 模拟的存储器。

DUT 支持四类主要功能：

1. direct read：CPU 通过 DUT 读取下游地址。
2. direct write：CPU 通过 DUT 写入下游地址。
3. DMA initialization：从 `dma_src` 开始，连续写入指定数据。
4. DMA copy：从 `dma_src` 连续读取，再写入 `dma_dst`。

## 2. 地址区域

DUT 使用 `paddr[31:28]` 判断访问类型：

| 高 4 bit | 名称 | 含义 |
|---|---|---|
| `4'h0` | `DIRECT_ADDR` | direct read/write，低 28 bit 作为下游地址 |
| `4'h1` | `DMA_SRC` | DMA 源地址寄存器 |
| `4'h2` | `DMA_DST` | DMA 目标地址寄存器 |
| `4'h3` | `DMA_LEN` | DMA 长度寄存器 |
| `4'h4` | `DMA_INIT` | 写入初始化数据并启动 DMA initialization |
| `4'h8` | `DMA_COPY` | 启动 DMA copy |
| `4'hf` | `DMA_INT` | DMA 中断状态寄存器 |

因此 `4'h5～4'h7` 和 `4'h9～4'he` 是当前设计未定义的保留区域，访问这些区域会产生 `invalid_op`。

## 3. 两个 interface 和虚接口

`top.sv` 实例化了两个真实接口：

```systemverilog
apb_intf APB_MINTF(clk);
apb_intf APB_SINTF(clk);
```

它们分别通过 `config_db` 传给 UVM environment：

```systemverilog
uvm_config_db#(virtual apb_intf)::set(
    null, "uvm_test_top.apb_device_env_1",
    "APB_VMINTF", APB_MINTF
);

uvm_config_db#(virtual apb_intf)::set(
    null, "uvm_test_top.apb_device_env_1",
    "APB_VSINTF", APB_SINTF
);
```

对应关系为：

| UVM 虚接口变量 | 指向的真实接口 | 用途 |
|---|---|---|
| `APB_VMINTF` | `APB_MINTF` | master agent 驱动和观察 CPU 到 DUT 的上游总线 |
| `APB_VSINTF` | `APB_SINTF` | slave agent 观察 DUT 请求，并驱动下游响应 |

虚接口只是 class 中的句柄，不会创建第二套信号。真正的信号始终位于 `top` 中的 interface 实例。

## 4. UVM 验证环境的数据流

### 4.1 上游 master 主动发送请求

```text
master sequence
      ↓ req
master sequencer
      ↓
master driver
      ↓ APB_MINTF
     DUT
      ↓ 已完成事务
master monitor
      ↓ analysis_port
coverage
```

master sequence 负责产生 CPU 请求，master driver 将 transaction 转换成 APB setup/access 时序。master monitor 只在：

```systemverilog
psel && penable && pready
```

成立时发布 transaction，因此 coverage 收到的是总线上已经完成的真实事务，而不是 sequence 原本计划发送的请求。

### 4.2 下游 slave 被动响应 DUT

```text
DUT发出下游setup请求
        ↓ APB_SINTF
slave monitor
        ↓ analysis_port
slave sequencer中的analysis_fifo
        ↓ fifo.get(item)
slave sequence / memory model
        ↓ 生成响应req
slave sequencer
        ↓
slave driver
        ↓ pready、prdata
DUT收到下游响应
```

这是一条 reactive slave 链路：slave sequence 不是凭空随机产生一个新的存储器请求，而是先等待 DUT 发出请求，再根据请求生成响应。

## 5. Master agent 各组件作用

### 5.1 `apb_master_item`

```systemverilog
rand bit [31:0] addr;
rand bit        write;
rand bit [31:0] data;
rand bit [3:0]  addr_delay;
rand bit [3:0]  ready_delay;
```

其中 `addr`、`write` 和 `data` 表示 CPU 请求；`addr_delay` 控制 driver 在发起事务前等待多少拍。当前 master driver 没有使用 `ready_delay`，因为 `pready` 是 DUT 的输出，master 不能决定 DUT 的响应延迟。

### 5.2 `apb_device_master_item`

它继承自 `apb_master_item`，增加 DUT 专用约束：

```systemverilog
constraint c_feature {
    addr[31:28] inside {[4'h0:4'h4], 4'h8, 4'hf};
    (addr[31:28] == 4'h4) -> (write == 1'b1);
    (addr[31:28] == 4'h8) -> (write == 1'b1);
}
```

`test` 中使用 factory override：

```systemverilog
apb_master_item::type_id::set_type_override(
    apb_device_master_item::get_type()
);
```

因此 sequence 虽然声明的是 `apb_master_item`，通过 factory 创建时实际会得到带有 DUT 地址约束的 `apb_device_master_item`。

### 5.3 `apb_master_basic_sequence`

sequence 首先执行 10 个约束随机事务，然后执行两个定向事务：

```systemverilog
repeat (10) `uvm_do(req)
`uvm_do_with(req, {write == 0; addr == 32'habcd;})
newData = req.data + 1;
`uvm_do_with(req, {
    write == 1;
    addr == 32'habcd;
    data == newData;
})
```

读取完成后，master driver 会把 `prdata` 写回 `req.data`，所以 `newData = req.data + 1` 使用的是实际读取结果。随后 sequence 将读取值加 1，再写回相同地址。

### 5.4 `apb_master_driver`

driver 完成标准 APB 传输：

1. 等待 `addr_delay`。
2. setup 阶段驱动 `paddr/pwrite/psel/pwdata`，保持 `penable=0`。
3. 下一拍进入 access 阶段，将 `penable=1`。
4. 如果 `pready=0`，保持控制和数据不变并继续等待。
5. `pready=1` 时完成传输；读操作采集 `prdata`。
6. 拉低 `psel` 和 `penable`。

### 5.5 `apb_master_monitor`

monitor 在每个完成握手点创建一个新对象：

```systemverilog
if (psel && penable && pready) begin
    apb_master_item item = new("item");
    item.addr  = paddr;
    item.write = pwrite;
    item.data  = item.write ? pwdata : prdata;
    aPort.write(item);
end
```

写事务采集 `pwdata`，读事务采集 `prdata`，因此统一使用 `item.data` 表示这次总线传输真正携带的数据。

## 6. Slave agent 各组件作用

### 6.1 Slave monitor 为什么存在

在这个 DUT 中，下游请求由 DUT 发起，所以 slave monitor 用来观察 DUT 发出的：

```text
slave_paddr
slave_pwrite
slave_psel
slave_penable
slave_pwdata
```

它在 setup 阶段：

```systemverilog
slave_psel && !slave_penable
```

采集请求并创建 `apb_slave_item`。这里采集 setup 而不是完成握手，是因为 reactive slave 必须先知道 DUT 要访问什么，之后才能生成 `pready/prdata` 响应。

### 6.2 Slave sequencer 中的 analysis FIFO

`apb_slave_sequencer` 同时具有常规 sequencer 功能和一条 analysis 接收链路：

```systemverilog
uvm_analysis_export #(apb_slave_item) aExport;
uvm_tlm_analysis_fifo #(apb_slave_item) fifo;
```

slave monitor 的 `aPort` 连接到 `aExport`，再连接到 FIFO。FIFO 使 monitor 和 sequence 解耦：monitor 可以继续观察总线，sequence 则通过阻塞式的：

```systemverilog
p_sequencer.fifo.get(item);
```

等待下一条 DUT 请求。

### 6.3 Slave sequence 的 memory model

slave sequence 使用关联数组模拟存储器：

```systemverilog
reg [31:0] memory [bit[31:0]];
```

收到写请求时，将 `item.data` 保存到 `memory[item.addr]`；收到读请求时，如果地址存在就返回原数据，否则返回 0：

```systemverilog
read_data = memory.exists(item.addr)
          ? memory[item.addr]
          : 0;
```

### 6.4 Slave driver 只应驱动什么

在下游 APB 上，DUT 是 master，UVM slave driver 是 slave，所以 driver 只负责驱动从设备方向的信号：

```text
pready：从设备何时完成传输
prdata：读事务返回的数据
```

代码行为是先等待随机的 `ready_delay`，然后拉高 `pready` 一拍；只有读请求才驱动 `prdata`：

```systemverilog
repeat (req_item.ready_delay) @(APB_VSINTF.slave_cb);
APB_VSINTF.slave_cb.pready <= 1'b1;
if (!req_item.write)
    APB_VSINTF.slave_cb.prdata <= req_item.data;
```

`write` 字段不是让 slave 去驱动 `pwrite`，而是告诉 slave driver：DUT 发来的这次请求属于写还是读，从而决定是否需要返回 `prdata`。

## 7. Monitor item 与 sequence req 的区别

slave sequence 中存在两个不同对象：

```systemverilog
apb_slave_item item;  // 从monitor/FIFO取得的DUT请求
req;                  // sequence生成并送给driver的响应item
```

`item` 的字段来自 DUT 总线，不应该被随机化；`req` 是送给 slave driver 的响应对象，可以随机化 `ready_delay`，但请求方向必须与 `item.write` 一致。

当前代码存在问题：

```systemverilog
if (item.write)
    `uvm_do(req)
else
    `uvm_do_with(req, {data == read_data;})
```

因为 `req.write` 是 `rand` 字段，上述两条宏都会随机化它。可能出现 DUT 实际发出写请求，但 `req.write` 被随机成 0，slave driver 便会错误地把它当成读请求。

至少应当约束为：

```systemverilog
if (item.write) begin
    `uvm_do_with(req, {
        write == 1'b1;
    })
    memory[item.addr] = item.data;
end
else begin
    read_data = memory.exists(item.addr) ? memory[item.addr] : 0;
    `uvm_do_with(req, {
        write == 1'b0;
        data  == read_data;
    })
end
```

这里真正适合随机的是 slave 的响应延迟，而不是 DUT 已经确定的请求方向。

## 8. Transaction 对象的创建、传递和释放

### 8.1 为什么 monitor 每次传输都要 `new`

推荐写法是：

```systemverilog
forever begin
    ...
    if (transfer_complete) begin
        apb_master_item item = new("item");
        ...
        aPort.write(item);
    end
end
```

这样每次传输都有独立对象。假设先后观察到 A、B 两笔传输，FIFO 中保存的是：

```text
FIFO → [对象A句柄] [对象B句柄]
```

如果把 `new` 放到 `forever` 外，只创建一个对象并不断修改，FIFO 中可能出现：

```text
FIFO → [同一个对象句柄] [同一个对象句柄]
```

当 monitor 修改它来表示事务 B 时，尚未处理的事务 A 也会表现成 B 的内容，因为两个位置指向同一块对象内存。

### 8.2 `analysis_port.write()` 传递的是句柄

UVM analysis port 默认不会自动深拷贝 transaction，而是把对象句柄传给所有接收者。接收者如果只立即读取对象，通常没有问题；如果需要长期保存，而生产者可能复用对象，则应该先 `clone()` 或 `copy()`。

当前 monitor 每笔传输都创建新对象，因此 FIFO 保存不同句柄，是安全的。

### 8.3 对象什么时候被释放

SystemVerilog class 对象通过句柄访问，并由仿真器管理内存。对象不会因为 monitor 内部代码块结束就立刻消失；只要 FIFO、sequence、scoreboard 或其他组件还保存着句柄，对象就仍然存在。

以 monitor 到 FIFO 为例：

```text
monitor new(item)
      ↓
aPort.write(item)
      ↓
FIFO保存item句柄
      ↓
monitor局部句柄离开作用域，但FIFO仍引用对象
      ↓
sequence执行fifo.get(item)
      ↓
对象句柄从FIFO转移给sequence
      ↓
sequence以后覆盖/清空该句柄，且没有其他引用
      ↓
对象成为可回收对象，由仿真器自动释放
```

所以 monitor 创建很多 item 不代表它们永久占用内存。真正导致对象无法回收的情况，是某个长期存在的队列、关联数组或组件一直保存对象句柄而不删除。

UVM component 与 transaction 不同：component 通常在 build phase 创建，并在整个仿真期间存在；transaction 是短生命周期对象，会随着引用消失而回收。

## 9. DUT direct read/write 的握手关系

### 9.1 Direct write

CPU 写数据已经通过 `pwdata` 提供给 DUT，因此 DUT 可以把数据转发给下游。当前 RTL 中，上游 `pready` 的写响应主要取决于随机 `pready_delay` 和 `!is_dma_busy`，不依赖 `slave_pready`：

```verilog
else if (!is_dma_read && psel && penable &&
         pready_delay == 0 && !is_dma_busy)
    pready <= 1;
```

因此 direct write 的上游完成和下游完成是两条相对独立的握手，可能上游先完成，也可能下游先完成。

### 9.2 Direct read

CPU 读取的数据来自下游 `slave_prdata`，所以 DUT 必须先等下游读完成：

```verilog
else if (is_dma_read && slave_psel && slave_penable &&
         !dma_direct_cnt && slave_pready &&
         !(dma_init_active | dma_copy_active_p1 | dma_copy_active_p2))
    pready <= 1;
```

下游 `slave_pready` 到达后，DUT 捕获 `slave_prdata`，再通过上游 `prdata/pready` 完成 CPU 读事务。

### 9.3 `dma_direct_cnt` 的作用

代码定义了两个事件：

```verilog
dma_cnt_push：上游CPU侧握手完成
dma_cnt_pop ：下游slave侧握手完成
```

`dma_direct_cnt` 在只有一侧完成时翻转，在两侧同时完成或都未完成时保持：

| `push` | `pop` | `dma_direct_cnt` |
|---:|---:|---|
| 0 | 0 | 保持 |
| 0 | 1 | 翻转 |
| 1 | 0 | 翻转 |
| 1 | 1 | 保持 |

它可以理解成“上下游完成状态是否暂时不成对”的标记：先完成一侧时变为 1，另一侧随后完成时再回到 0。它不是 APB 协议信号，而是 DUT 内部用于协调双侧事务的状态。

## 10. DMA initialization 和 copy

### 10.1 DMA initialization

CPU 先配置 `DMA_SRC` 和 `DMA_LEN`，再向 `DMA_INIT` 写入初始化数据。DUT 保存：

```verilog
dma_init_value <= pwdata;
dma_init_active <= 1;
```

运行期间，DUT 从 `dma_src` 指定地址开始，向下游连续发起写请求：

```text
地址：dma_src、dma_src+1、dma_src+2……
数据：dma_init_value
次数：由dma_len控制
```

每次下游操作推进时更新地址和长度，最后一个元素完成后退出 `dma_init_active`。

### 10.2 DMA copy

CPU 先配置 `DMA_SRC`、`DMA_DST` 和 `DMA_LEN`，再写 `DMA_COPY` 启动复制。copy 使用两个阶段：

```text
dma_copy_active_p1：从dma_src读取slave_prdata
dma_copy_active_p2：把读取数据写到dma_dst
```

读回的数据暂存在 `dma_init_value`：

```verilog
else if (dma_copy_active_p1 && slave_pready)
    dma_init_value <= slave_prdata;
```

随后写阶段将数据送到目标地址，并推进 `dma_src`、`dma_dst` 和 `dma_len`。

## 11. DMA_LEN 行为

`dma_len` 只有 5 bit：

```verilog
reg [4:0] dma_len;
```

但 CPU 的 `pwdata` 是 32 bit。当前写法会把 `pwdata[4:0]` 截断保存：

```verilog
dma_len <= pwdata;
```

与此同时，完整的 32 bit `pwdata` 会被检查：

```verilog
pwdata == 0 || pwdata > 16
```

因此当前 RTL 对非法长度的实际行为是：产生 `dma_int[2]`，但仍将低 5 bit 写入 `dma_len`。验证时应分别覆盖合法值 `1～16` 和非法值 `0、17～32'hffff_ffff`，不能把所有 sequence 永久限制成合法输入，否则无法验证错误处理。

详细优化建议见 [后续优化方案.md](./后续优化方案.md)。

## 12. DMA 中断逻辑

`dma_int` 是 32 bit 中断状态寄存器，目前使用低 4 bit：

| 位 | 事件 | 当前代码含义 |
|---|---|---|
| `dma_int[3]` | `invalid_op` | 访问无效地址区域 |
| `dma_int[2]` | `invalid_value_of_length` | DMA_LEN 为 0 或大于 16 |
| `dma_int[1]` | `copy_overlap` | DMA copy 源/目标区间重叠 |
| `dma_int[0]` | `dma_init_copy_done` | init/copy 期间出现下游完成响应 |

新事件通过按位或合并到已有状态：

```verilog
dma_int_nxt = dma_int | dma_int_update;
```

所以已有中断不会因为新事件消失而自动清零。CPU 对 `DMA_INT` 执行写操作时，采用 W1C（write one to clear）：

```verilog
dma_int_nxt = dma_int & ~pwdata;
```

`pwdata` 某位写 1 会清除对应中断位，写 0 则保持。硬件不强制 CPU 先读后写；软件通常先读 `dma_int` 确认原因，再写对应掩码清除。

总中断输出为：

```verilog
assign apb_device_int = |dma_int;
```

只要 `dma_int` 任意一位为 1，`apb_device_int` 就保持为 1。

## 13. Coverage 代码理解

当前基础 coverage 在上游 monitor 完成事务后采样：

```systemverilog
apb_device_feature = {item.addr[31:28], item.write};
cov_apb_device_feature.sample();
```

5 bit 特征由“地址高 4 bit + 读写方向”构成，因此：

```systemverilog
wildcard bins dma_interrupt = {5'b1111?};
```

表示 `DMA_INT` 区域的读和写都可以命中这个 bin，`?` 同时匹配 `write=0` 和 `write=1`。

### Coverage 与正确性检查的区别

Coverage 只回答“场景有没有发生”，不能回答“DUT 结果是否正确”。例如命中 DMA copy bin 只能证明启动命令出现过；数据是否复制正确、传输次数是否正确，需要 scoreboard/reference model 判断。

完整验证应当包含：

```text
sequence：产生场景
monitor：采集真实结果
scoreboard：检查结果正确性
coverage：统计场景完整性
assertion：检查协议和时序规则
```

### enum 和分类函数

复杂 coverage 可以先把原始数据分类成 enum，例如：

```systemverilog
typedef enum {
    LEN_ZERO,
    LEN_MINIMUM,
    LEN_NORMAL,
    LEN_MAXIMUM,
    LEN_TOO_LARGE
} length_class_e;
```

分类函数把 32 bit 长度转换成其中一个有限类别，再传入 covergroup 的 `sample()`。这样 bins 直接描述功能语义，而不需要在多个位置重复写数值范围。

### `option.per_instance` 和 `option.goal`

```systemverilog
option.per_instance = 1;
option.goal = 100;
```

`per_instance=1` 表示同一种 covergroup 如果创建多个实例，工具保留每个实例自己的覆盖结果；不设置时，工具通常更强调类型合并结果。`goal=100` 表示该 covergroup 的目标覆盖率为 100%，它只影响报告目标，不会自动产生激励。

### `uvm_subscriber` 和 analysis imp

`uvm_subscriber#(T)` 本身继承自 `uvm_component`，并自带一个接收 analysis transaction 的入口，适合直接实现：

```systemverilog
function void write(T item);
```

如果一个 coverage component 同时接收 master item 和 slave item，可以使用两个 analysis implementation。宏：

```systemverilog
`uvm_analysis_imp_decl(_apb_master_cov_full)
```

会生成带有该后缀的 analysis imp 类型，并约定将收到的数据转发到：

```systemverilog
write_apb_master_cov_full(...)
```

后缀由用户自己命名，不是 UVM 内置的固定长名称。

## 14. 当前代码已确认的注意点

### 14.1 Interface clocking block 多驱动

同一个 `apb_intf` 同时包含 `master_cb` 和 `slave_cb`。每个 interface 实例都会同时包含这两个 clocking block，即使某个实例只打算用于 master 或 slave。

因此：

- `APB_SINTF.pwdata` 一方面由 DUT 的 `slave_pwdata` 结构化驱动，另一方面被 `master_cb` 声明为 output。
- `APB_MINTF.prdata` 一方面由 DUT 的 `prdata` 结构化驱动，另一方面被 `slave_cb` 声明为 output。

这会产生 structural/procedural driver combination 警告，并可能在仿真中造成 `X` 或未来版本编译错误。建议将 master interface 和 slave interface 拆开，使每个实例只包含符合自身角色的 clocking block。

### 14.2 Slave response 的 `write` 随机化

slave sequence 创建响应 `req` 时没有约束 `req.write`，可能导致 driver 错误判断读写方向。应该保留 `item.write` 的真实方向，只随机化 `ready_delay` 等从设备可控制的响应属性。

### 14.3 DMA_LEN 非法访问副作用

当前非法长度会触发中断但仍写入截断值，而且 `invalid_value_of_length` 没有判断 `pwrite`，读取 DMA_LEN 也存在误触发风险。详细内容已经记录在后续优化方案中。

### 14.4 `dma_init_copy_done` 的命名与实际条件

当前条件是：

```verilog
(dma_init_active | dma_copy_active_p2) & slave_pready
```

它可能在多长度 DMA 的某一次下游传输完成时就置位，而不一定只在整个 DMA 命令最后一次传输完成时置位。验证时应根据预期规格确认 bit0 究竟表示“单次下游完成”还是“整个 DMA 完成”。

### 14.5 组合逻辑默认赋值

`dma_int_nxt` 和 `prdata_nxt` 的组合逻辑没有完整默认分支，工具可能推导锁存器或产生告警。即使当前时序更新条件在多数情况下能避免明显错误，后续 RTL 优化仍应补充默认赋值。

### 14.6 暂未使用的变量或字段

- DUT 中 `int_status` 只声明但未使用。
- master item 的 `ready_delay` 当前未使用。
- slave item 的 `addr_delay` 当前未使用。

这些内容可以保留作扩展，也可以在接口和验证计划稳定后清理，减少误解。

## 15. 当前验证环境距离全流程验证还缺少什么

当前工程已经具备 master/slave agent、基础 sequence、monitor、memory model 和基础 coverage，但完整闭环仍应补充：

1. scoreboard 和 reference model：检查 direct read/write、DMA init/copy 的数据和传输次数。
2. 系统化定向 sequence：覆盖合法配置、边界值、非法操作、中断和 busy 期间访问。
3. 完整 functional coverage：除命令类型外，还要覆盖长度、地址关系、中断、等待周期和交叉场景。
4. APB 协议 assertion：检查 setup/access、等待期间信号稳定、握手和复位行为。
5. interface 多驱动修复：避免仿真出现不可靠值。
6. slave response 随机化修复：保证 response 方向与 DUT 请求一致。
7. regression 与 coverage closure：多随机种子运行，根据 coverage hole 增加定向测试。

## 16. 一句话总结各组件

| 组件 | 一句话作用 |
|---|---|
| master sequence | 决定 CPU 想执行什么操作 |
| master driver | 把 CPU transaction 转换成上游 APB 时序 |
| master monitor | 记录上游真正完成的事务 |
| slave monitor | 观察 DUT 向下游发出了什么请求 |
| slave sequence | 根据下游请求查询/更新 memory model 并生成响应 |
| slave driver | 向 DUT 返回 `pready/prdata` |
| scoreboard | 判断 DUT 的数据与行为是否正确 |
| coverage | 判断计划中的功能场景是否出现 |
| assertion | 判断 APB 时序规则是否被违反 |

